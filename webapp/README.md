# Administrator Dashboard Web App
## How to use the app
* To install required dependencies, navigate to the `webapp` folder and type `npm i` into the command line. 
* After installing dependencies, type `npm run dev` to view the app
* To test, type `npm run test`